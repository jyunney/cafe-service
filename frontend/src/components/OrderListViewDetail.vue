<template>

    <v-card outlined>
        <v-card-title>
            OrderList # {{$route.params.id }}
        </v-card-title>

        <v-card-text>
            <div>
                <Number label="OrderId" v-model="item.orderId" :editMode="editMode" @change="change" />
            </div>
            <div>
                <Number label="CustumerId" v-model="item.custumerId" :editMode="editMode" @change="change" />
            </div>
            <div>
                <Number label="CafeId" v-model="item.cafeId" :editMode="editMode" @change="change" />
            </div>
            <div>
                <Number label="MenuId" v-model="item.menuId" :editMode="editMode" @change="change" />
            </div>
            <div>
                <Number label="Qty" v-model="item.qty" :editMode="editMode" @change="change" />
            </div>
            <div>
                <Number label="TotalPrice" v-model="item.totalPrice" :editMode="editMode" @change="change" />
            </div>
            <div>
                <String label="Status" v-model="item.status" :editMode="editMode" @change="change" />
            </div>
            <div>
                <Date label="OrderDate" v-model="item.orderDate" :editMode="editMode" @change="change" />
            </div>
        </v-card-text>
    </v-card>

</template>

<script>
  const axios = require('axios').default;

  export default {
    name: 'OrderListViewDetail',
    props: {
      value: Object,
    },
    data: () => ({
        item : [],
    }),
    async created() {
      var params = this.$route.params;
      var temp = await axios.get(axios.fixUrl('/orderLists/' + params.id))

      this.item = temp.data;

    },
    methods: {
    }
  }
</script>

